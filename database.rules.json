{
  "rules": {
    "users": {
      ".read": "auth != null", 
      ".indexOn": ["score", "referralCount"],
      "$uid": {
        ".read": "$uid === auth.uid",
        
        ".write": "$uid === auth.uid",

        "name": {
           ".validate": "newData.isString() && newData.val().length >= 3 && newData.val().length <= 20"
        }
      }
    }
  }
}
